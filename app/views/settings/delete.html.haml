= flash_message :cant_delete_last_root, context: :danger
%h2= t '.title'
%p.lead= t '.lead'
%p= t '.description'

%div.text-center
  = button_to settings_delete_path, method: :delete, context: :danger, data: { confirm: t('.confirm') } do
    = icon :trash
    = t '.button'
